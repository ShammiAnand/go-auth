// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/shammianand/go-auth/ent/roles"
	"github.com/shammianand/go-auth/ent/userroles"
	"github.com/shammianand/go-auth/ent/users"
)

// UserRoles is the model entity for the UserRoles schema.
type UserRoles struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// RoleID holds the value of the "role_id" field.
	RoleID int `json:"role_id,omitempty"`
	// User who assigned this role
	AssignedBy *uuid.UUID `json:"assigned_by,omitempty"`
	// AssignedAt holds the value of the "assigned_at" field.
	AssignedAt time.Time `json:"assigned_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserRolesQuery when eager-loading is set.
	Edges        UserRolesEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserRolesEdges holds the relations/edges for other nodes in the graph.
type UserRolesEdges struct {
	// User holds the value of the user edge.
	User *Users `json:"user,omitempty"`
	// Role holds the value of the role edge.
	Role *Roles `json:"role,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserRolesEdges) UserOrErr() (*Users, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: users.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// RoleOrErr returns the Role value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserRolesEdges) RoleOrErr() (*Roles, error) {
	if e.Role != nil {
		return e.Role, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: roles.Label}
	}
	return nil, &NotLoadedError{edge: "role"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserRoles) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userroles.FieldAssignedBy:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case userroles.FieldRoleID:
			values[i] = new(sql.NullInt64)
		case userroles.FieldAssignedAt:
			values[i] = new(sql.NullTime)
		case userroles.FieldID, userroles.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserRoles fields.
func (ur *UserRoles) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userroles.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ur.ID = *value
			}
		case userroles.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				ur.UserID = *value
			}
		case userroles.FieldRoleID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field role_id", values[i])
			} else if value.Valid {
				ur.RoleID = int(value.Int64)
			}
		case userroles.FieldAssignedBy:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field assigned_by", values[i])
			} else if value.Valid {
				ur.AssignedBy = new(uuid.UUID)
				*ur.AssignedBy = *value.S.(*uuid.UUID)
			}
		case userroles.FieldAssignedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field assigned_at", values[i])
			} else if value.Valid {
				ur.AssignedAt = value.Time
			}
		default:
			ur.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserRoles.
// This includes values selected through modifiers, order, etc.
func (ur *UserRoles) Value(name string) (ent.Value, error) {
	return ur.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserRoles entity.
func (ur *UserRoles) QueryUser() *UsersQuery {
	return NewUserRolesClient(ur.config).QueryUser(ur)
}

// QueryRole queries the "role" edge of the UserRoles entity.
func (ur *UserRoles) QueryRole() *RolesQuery {
	return NewUserRolesClient(ur.config).QueryRole(ur)
}

// Update returns a builder for updating this UserRoles.
// Note that you need to call UserRoles.Unwrap() before calling this method if this UserRoles
// was returned from a transaction, and the transaction was committed or rolled back.
func (ur *UserRoles) Update() *UserRolesUpdateOne {
	return NewUserRolesClient(ur.config).UpdateOne(ur)
}

// Unwrap unwraps the UserRoles entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ur *UserRoles) Unwrap() *UserRoles {
	_tx, ok := ur.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserRoles is not a transactional entity")
	}
	ur.config.driver = _tx.drv
	return ur
}

// String implements the fmt.Stringer.
func (ur *UserRoles) String() string {
	var builder strings.Builder
	builder.WriteString("UserRoles(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ur.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", ur.UserID))
	builder.WriteString(", ")
	builder.WriteString("role_id=")
	builder.WriteString(fmt.Sprintf("%v", ur.RoleID))
	builder.WriteString(", ")
	if v := ur.AssignedBy; v != nil {
		builder.WriteString("assigned_by=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("assigned_at=")
	builder.WriteString(ur.AssignedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserRolesSlice is a parsable slice of UserRoles.
type UserRolesSlice []*UserRoles
