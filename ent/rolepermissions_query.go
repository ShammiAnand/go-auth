// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/shammianand/go-auth/ent/permissions"
	"github.com/shammianand/go-auth/ent/predicate"
	"github.com/shammianand/go-auth/ent/rolepermissions"
	"github.com/shammianand/go-auth/ent/roles"
)

// RolePermissionsQuery is the builder for querying RolePermissions entities.
type RolePermissionsQuery struct {
	config
	ctx            *QueryContext
	order          []rolepermissions.OrderOption
	inters         []Interceptor
	predicates     []predicate.RolePermissions
	withRole       *RolesQuery
	withPermission *PermissionsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RolePermissionsQuery builder.
func (rpq *RolePermissionsQuery) Where(ps ...predicate.RolePermissions) *RolePermissionsQuery {
	rpq.predicates = append(rpq.predicates, ps...)
	return rpq
}

// Limit the number of records to be returned by this query.
func (rpq *RolePermissionsQuery) Limit(limit int) *RolePermissionsQuery {
	rpq.ctx.Limit = &limit
	return rpq
}

// Offset to start from.
func (rpq *RolePermissionsQuery) Offset(offset int) *RolePermissionsQuery {
	rpq.ctx.Offset = &offset
	return rpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rpq *RolePermissionsQuery) Unique(unique bool) *RolePermissionsQuery {
	rpq.ctx.Unique = &unique
	return rpq
}

// Order specifies how the records should be ordered.
func (rpq *RolePermissionsQuery) Order(o ...rolepermissions.OrderOption) *RolePermissionsQuery {
	rpq.order = append(rpq.order, o...)
	return rpq
}

// QueryRole chains the current query on the "role" edge.
func (rpq *RolePermissionsQuery) QueryRole() *RolesQuery {
	query := (&RolesClient{config: rpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rolepermissions.Table, rolepermissions.FieldID, selector),
			sqlgraph.To(roles.Table, roles.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, rolepermissions.RoleTable, rolepermissions.RoleColumn),
		)
		fromU = sqlgraph.SetNeighbors(rpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPermission chains the current query on the "permission" edge.
func (rpq *RolePermissionsQuery) QueryPermission() *PermissionsQuery {
	query := (&PermissionsClient{config: rpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rolepermissions.Table, rolepermissions.FieldID, selector),
			sqlgraph.To(permissions.Table, permissions.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, rolepermissions.PermissionTable, rolepermissions.PermissionColumn),
		)
		fromU = sqlgraph.SetNeighbors(rpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RolePermissions entity from the query.
// Returns a *NotFoundError when no RolePermissions was found.
func (rpq *RolePermissionsQuery) First(ctx context.Context) (*RolePermissions, error) {
	nodes, err := rpq.Limit(1).All(setContextOp(ctx, rpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rolepermissions.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rpq *RolePermissionsQuery) FirstX(ctx context.Context) *RolePermissions {
	node, err := rpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RolePermissions ID from the query.
// Returns a *NotFoundError when no RolePermissions ID was found.
func (rpq *RolePermissionsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rpq.Limit(1).IDs(setContextOp(ctx, rpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rolepermissions.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rpq *RolePermissionsQuery) FirstIDX(ctx context.Context) int {
	id, err := rpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RolePermissions entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RolePermissions entity is found.
// Returns a *NotFoundError when no RolePermissions entities are found.
func (rpq *RolePermissionsQuery) Only(ctx context.Context) (*RolePermissions, error) {
	nodes, err := rpq.Limit(2).All(setContextOp(ctx, rpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rolepermissions.Label}
	default:
		return nil, &NotSingularError{rolepermissions.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rpq *RolePermissionsQuery) OnlyX(ctx context.Context) *RolePermissions {
	node, err := rpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RolePermissions ID in the query.
// Returns a *NotSingularError when more than one RolePermissions ID is found.
// Returns a *NotFoundError when no entities are found.
func (rpq *RolePermissionsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rpq.Limit(2).IDs(setContextOp(ctx, rpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rolepermissions.Label}
	default:
		err = &NotSingularError{rolepermissions.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rpq *RolePermissionsQuery) OnlyIDX(ctx context.Context) int {
	id, err := rpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RolePermissionsSlice.
func (rpq *RolePermissionsQuery) All(ctx context.Context) ([]*RolePermissions, error) {
	ctx = setContextOp(ctx, rpq.ctx, "All")
	if err := rpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RolePermissions, *RolePermissionsQuery]()
	return withInterceptors[[]*RolePermissions](ctx, rpq, qr, rpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rpq *RolePermissionsQuery) AllX(ctx context.Context) []*RolePermissions {
	nodes, err := rpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RolePermissions IDs.
func (rpq *RolePermissionsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if rpq.ctx.Unique == nil && rpq.path != nil {
		rpq.Unique(true)
	}
	ctx = setContextOp(ctx, rpq.ctx, "IDs")
	if err = rpq.Select(rolepermissions.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rpq *RolePermissionsQuery) IDsX(ctx context.Context) []int {
	ids, err := rpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rpq *RolePermissionsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rpq.ctx, "Count")
	if err := rpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rpq, querierCount[*RolePermissionsQuery](), rpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rpq *RolePermissionsQuery) CountX(ctx context.Context) int {
	count, err := rpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rpq *RolePermissionsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rpq.ctx, "Exist")
	switch _, err := rpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rpq *RolePermissionsQuery) ExistX(ctx context.Context) bool {
	exist, err := rpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RolePermissionsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rpq *RolePermissionsQuery) Clone() *RolePermissionsQuery {
	if rpq == nil {
		return nil
	}
	return &RolePermissionsQuery{
		config:         rpq.config,
		ctx:            rpq.ctx.Clone(),
		order:          append([]rolepermissions.OrderOption{}, rpq.order...),
		inters:         append([]Interceptor{}, rpq.inters...),
		predicates:     append([]predicate.RolePermissions{}, rpq.predicates...),
		withRole:       rpq.withRole.Clone(),
		withPermission: rpq.withPermission.Clone(),
		// clone intermediate query.
		sql:  rpq.sql.Clone(),
		path: rpq.path,
	}
}

// WithRole tells the query-builder to eager-load the nodes that are connected to
// the "role" edge. The optional arguments are used to configure the query builder of the edge.
func (rpq *RolePermissionsQuery) WithRole(opts ...func(*RolesQuery)) *RolePermissionsQuery {
	query := (&RolesClient{config: rpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rpq.withRole = query
	return rpq
}

// WithPermission tells the query-builder to eager-load the nodes that are connected to
// the "permission" edge. The optional arguments are used to configure the query builder of the edge.
func (rpq *RolePermissionsQuery) WithPermission(opts ...func(*PermissionsQuery)) *RolePermissionsQuery {
	query := (&PermissionsClient{config: rpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rpq.withPermission = query
	return rpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RoleID int `json:"role_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RolePermissions.Query().
//		GroupBy(rolepermissions.FieldRoleID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rpq *RolePermissionsQuery) GroupBy(field string, fields ...string) *RolePermissionsGroupBy {
	rpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RolePermissionsGroupBy{build: rpq}
	grbuild.flds = &rpq.ctx.Fields
	grbuild.label = rolepermissions.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RoleID int `json:"role_id,omitempty"`
//	}
//
//	client.RolePermissions.Query().
//		Select(rolepermissions.FieldRoleID).
//		Scan(ctx, &v)
func (rpq *RolePermissionsQuery) Select(fields ...string) *RolePermissionsSelect {
	rpq.ctx.Fields = append(rpq.ctx.Fields, fields...)
	sbuild := &RolePermissionsSelect{RolePermissionsQuery: rpq}
	sbuild.label = rolepermissions.Label
	sbuild.flds, sbuild.scan = &rpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RolePermissionsSelect configured with the given aggregations.
func (rpq *RolePermissionsQuery) Aggregate(fns ...AggregateFunc) *RolePermissionsSelect {
	return rpq.Select().Aggregate(fns...)
}

func (rpq *RolePermissionsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rpq); err != nil {
				return err
			}
		}
	}
	for _, f := range rpq.ctx.Fields {
		if !rolepermissions.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rpq.path != nil {
		prev, err := rpq.path(ctx)
		if err != nil {
			return err
		}
		rpq.sql = prev
	}
	return nil
}

func (rpq *RolePermissionsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RolePermissions, error) {
	var (
		nodes       = []*RolePermissions{}
		_spec       = rpq.querySpec()
		loadedTypes = [2]bool{
			rpq.withRole != nil,
			rpq.withPermission != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RolePermissions).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RolePermissions{config: rpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rpq.withRole; query != nil {
		if err := rpq.loadRole(ctx, query, nodes, nil,
			func(n *RolePermissions, e *Roles) { n.Edges.Role = e }); err != nil {
			return nil, err
		}
	}
	if query := rpq.withPermission; query != nil {
		if err := rpq.loadPermission(ctx, query, nodes, nil,
			func(n *RolePermissions, e *Permissions) { n.Edges.Permission = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rpq *RolePermissionsQuery) loadRole(ctx context.Context, query *RolesQuery, nodes []*RolePermissions, init func(*RolePermissions), assign func(*RolePermissions, *Roles)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*RolePermissions)
	for i := range nodes {
		fk := nodes[i].RoleID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(roles.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "role_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rpq *RolePermissionsQuery) loadPermission(ctx context.Context, query *PermissionsQuery, nodes []*RolePermissions, init func(*RolePermissions), assign func(*RolePermissions, *Permissions)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*RolePermissions)
	for i := range nodes {
		fk := nodes[i].PermissionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(permissions.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "permission_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rpq *RolePermissionsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rpq.querySpec()
	_spec.Node.Columns = rpq.ctx.Fields
	if len(rpq.ctx.Fields) > 0 {
		_spec.Unique = rpq.ctx.Unique != nil && *rpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rpq.driver, _spec)
}

func (rpq *RolePermissionsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(rolepermissions.Table, rolepermissions.Columns, sqlgraph.NewFieldSpec(rolepermissions.FieldID, field.TypeInt))
	_spec.From = rpq.sql
	if unique := rpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rpq.path != nil {
		_spec.Unique = true
	}
	if fields := rpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rolepermissions.FieldID)
		for i := range fields {
			if fields[i] != rolepermissions.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rpq.withRole != nil {
			_spec.Node.AddColumnOnce(rolepermissions.FieldRoleID)
		}
		if rpq.withPermission != nil {
			_spec.Node.AddColumnOnce(rolepermissions.FieldPermissionID)
		}
	}
	if ps := rpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rpq *RolePermissionsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rpq.driver.Dialect())
	t1 := builder.Table(rolepermissions.Table)
	columns := rpq.ctx.Fields
	if len(columns) == 0 {
		columns = rolepermissions.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rpq.sql != nil {
		selector = rpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rpq.ctx.Unique != nil && *rpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rpq.predicates {
		p(selector)
	}
	for _, p := range rpq.order {
		p(selector)
	}
	if offset := rpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RolePermissionsGroupBy is the group-by builder for RolePermissions entities.
type RolePermissionsGroupBy struct {
	selector
	build *RolePermissionsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rpgb *RolePermissionsGroupBy) Aggregate(fns ...AggregateFunc) *RolePermissionsGroupBy {
	rpgb.fns = append(rpgb.fns, fns...)
	return rpgb
}

// Scan applies the selector query and scans the result into the given value.
func (rpgb *RolePermissionsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpgb.build.ctx, "GroupBy")
	if err := rpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RolePermissionsQuery, *RolePermissionsGroupBy](ctx, rpgb.build, rpgb, rpgb.build.inters, v)
}

func (rpgb *RolePermissionsGroupBy) sqlScan(ctx context.Context, root *RolePermissionsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rpgb.fns))
	for _, fn := range rpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rpgb.flds)+len(rpgb.fns))
		for _, f := range *rpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RolePermissionsSelect is the builder for selecting fields of RolePermissions entities.
type RolePermissionsSelect struct {
	*RolePermissionsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rps *RolePermissionsSelect) Aggregate(fns ...AggregateFunc) *RolePermissionsSelect {
	rps.fns = append(rps.fns, fns...)
	return rps
}

// Scan applies the selector query and scans the result into the given value.
func (rps *RolePermissionsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rps.ctx, "Select")
	if err := rps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RolePermissionsQuery, *RolePermissionsSelect](ctx, rps.RolePermissionsQuery, rps, rps.inters, v)
}

func (rps *RolePermissionsSelect) sqlScan(ctx context.Context, root *RolePermissionsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rps.fns))
	for _, fn := range rps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
