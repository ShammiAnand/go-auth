// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/shammianand/go-auth/ent/emaillogs"
	"github.com/shammianand/go-auth/ent/predicate"
)

// EmailLogsQuery is the builder for querying EmailLogs entities.
type EmailLogsQuery struct {
	config
	ctx        *QueryContext
	order      []emaillogs.OrderOption
	inters     []Interceptor
	predicates []predicate.EmailLogs
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EmailLogsQuery builder.
func (elq *EmailLogsQuery) Where(ps ...predicate.EmailLogs) *EmailLogsQuery {
	elq.predicates = append(elq.predicates, ps...)
	return elq
}

// Limit the number of records to be returned by this query.
func (elq *EmailLogsQuery) Limit(limit int) *EmailLogsQuery {
	elq.ctx.Limit = &limit
	return elq
}

// Offset to start from.
func (elq *EmailLogsQuery) Offset(offset int) *EmailLogsQuery {
	elq.ctx.Offset = &offset
	return elq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (elq *EmailLogsQuery) Unique(unique bool) *EmailLogsQuery {
	elq.ctx.Unique = &unique
	return elq
}

// Order specifies how the records should be ordered.
func (elq *EmailLogsQuery) Order(o ...emaillogs.OrderOption) *EmailLogsQuery {
	elq.order = append(elq.order, o...)
	return elq
}

// First returns the first EmailLogs entity from the query.
// Returns a *NotFoundError when no EmailLogs was found.
func (elq *EmailLogsQuery) First(ctx context.Context) (*EmailLogs, error) {
	nodes, err := elq.Limit(1).All(setContextOp(ctx, elq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{emaillogs.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (elq *EmailLogsQuery) FirstX(ctx context.Context) *EmailLogs {
	node, err := elq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EmailLogs ID from the query.
// Returns a *NotFoundError when no EmailLogs ID was found.
func (elq *EmailLogsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = elq.Limit(1).IDs(setContextOp(ctx, elq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{emaillogs.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (elq *EmailLogsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := elq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EmailLogs entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EmailLogs entity is found.
// Returns a *NotFoundError when no EmailLogs entities are found.
func (elq *EmailLogsQuery) Only(ctx context.Context) (*EmailLogs, error) {
	nodes, err := elq.Limit(2).All(setContextOp(ctx, elq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{emaillogs.Label}
	default:
		return nil, &NotSingularError{emaillogs.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (elq *EmailLogsQuery) OnlyX(ctx context.Context) *EmailLogs {
	node, err := elq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EmailLogs ID in the query.
// Returns a *NotSingularError when more than one EmailLogs ID is found.
// Returns a *NotFoundError when no entities are found.
func (elq *EmailLogsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = elq.Limit(2).IDs(setContextOp(ctx, elq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{emaillogs.Label}
	default:
		err = &NotSingularError{emaillogs.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (elq *EmailLogsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := elq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EmailLogsSlice.
func (elq *EmailLogsQuery) All(ctx context.Context) ([]*EmailLogs, error) {
	ctx = setContextOp(ctx, elq.ctx, "All")
	if err := elq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EmailLogs, *EmailLogsQuery]()
	return withInterceptors[[]*EmailLogs](ctx, elq, qr, elq.inters)
}

// AllX is like All, but panics if an error occurs.
func (elq *EmailLogsQuery) AllX(ctx context.Context) []*EmailLogs {
	nodes, err := elq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EmailLogs IDs.
func (elq *EmailLogsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if elq.ctx.Unique == nil && elq.path != nil {
		elq.Unique(true)
	}
	ctx = setContextOp(ctx, elq.ctx, "IDs")
	if err = elq.Select(emaillogs.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (elq *EmailLogsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := elq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (elq *EmailLogsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, elq.ctx, "Count")
	if err := elq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, elq, querierCount[*EmailLogsQuery](), elq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (elq *EmailLogsQuery) CountX(ctx context.Context) int {
	count, err := elq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (elq *EmailLogsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, elq.ctx, "Exist")
	switch _, err := elq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (elq *EmailLogsQuery) ExistX(ctx context.Context) bool {
	exist, err := elq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EmailLogsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (elq *EmailLogsQuery) Clone() *EmailLogsQuery {
	if elq == nil {
		return nil
	}
	return &EmailLogsQuery{
		config:     elq.config,
		ctx:        elq.ctx.Clone(),
		order:      append([]emaillogs.OrderOption{}, elq.order...),
		inters:     append([]Interceptor{}, elq.inters...),
		predicates: append([]predicate.EmailLogs{}, elq.predicates...),
		// clone intermediate query.
		sql:  elq.sql.Clone(),
		path: elq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EmailLogs.Query().
//		GroupBy(emaillogs.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (elq *EmailLogsQuery) GroupBy(field string, fields ...string) *EmailLogsGroupBy {
	elq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EmailLogsGroupBy{build: elq}
	grbuild.flds = &elq.ctx.Fields
	grbuild.label = emaillogs.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//	}
//
//	client.EmailLogs.Query().
//		Select(emaillogs.FieldUserID).
//		Scan(ctx, &v)
func (elq *EmailLogsQuery) Select(fields ...string) *EmailLogsSelect {
	elq.ctx.Fields = append(elq.ctx.Fields, fields...)
	sbuild := &EmailLogsSelect{EmailLogsQuery: elq}
	sbuild.label = emaillogs.Label
	sbuild.flds, sbuild.scan = &elq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EmailLogsSelect configured with the given aggregations.
func (elq *EmailLogsQuery) Aggregate(fns ...AggregateFunc) *EmailLogsSelect {
	return elq.Select().Aggregate(fns...)
}

func (elq *EmailLogsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range elq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, elq); err != nil {
				return err
			}
		}
	}
	for _, f := range elq.ctx.Fields {
		if !emaillogs.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if elq.path != nil {
		prev, err := elq.path(ctx)
		if err != nil {
			return err
		}
		elq.sql = prev
	}
	return nil
}

func (elq *EmailLogsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EmailLogs, error) {
	var (
		nodes = []*EmailLogs{}
		_spec = elq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EmailLogs).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EmailLogs{config: elq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, elq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (elq *EmailLogsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := elq.querySpec()
	_spec.Node.Columns = elq.ctx.Fields
	if len(elq.ctx.Fields) > 0 {
		_spec.Unique = elq.ctx.Unique != nil && *elq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, elq.driver, _spec)
}

func (elq *EmailLogsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(emaillogs.Table, emaillogs.Columns, sqlgraph.NewFieldSpec(emaillogs.FieldID, field.TypeUUID))
	_spec.From = elq.sql
	if unique := elq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if elq.path != nil {
		_spec.Unique = true
	}
	if fields := elq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, emaillogs.FieldID)
		for i := range fields {
			if fields[i] != emaillogs.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := elq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := elq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := elq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := elq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (elq *EmailLogsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(elq.driver.Dialect())
	t1 := builder.Table(emaillogs.Table)
	columns := elq.ctx.Fields
	if len(columns) == 0 {
		columns = emaillogs.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if elq.sql != nil {
		selector = elq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if elq.ctx.Unique != nil && *elq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range elq.predicates {
		p(selector)
	}
	for _, p := range elq.order {
		p(selector)
	}
	if offset := elq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := elq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EmailLogsGroupBy is the group-by builder for EmailLogs entities.
type EmailLogsGroupBy struct {
	selector
	build *EmailLogsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (elgb *EmailLogsGroupBy) Aggregate(fns ...AggregateFunc) *EmailLogsGroupBy {
	elgb.fns = append(elgb.fns, fns...)
	return elgb
}

// Scan applies the selector query and scans the result into the given value.
func (elgb *EmailLogsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, elgb.build.ctx, "GroupBy")
	if err := elgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmailLogsQuery, *EmailLogsGroupBy](ctx, elgb.build, elgb, elgb.build.inters, v)
}

func (elgb *EmailLogsGroupBy) sqlScan(ctx context.Context, root *EmailLogsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(elgb.fns))
	for _, fn := range elgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*elgb.flds)+len(elgb.fns))
		for _, f := range *elgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*elgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := elgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EmailLogsSelect is the builder for selecting fields of EmailLogs entities.
type EmailLogsSelect struct {
	*EmailLogsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (els *EmailLogsSelect) Aggregate(fns ...AggregateFunc) *EmailLogsSelect {
	els.fns = append(els.fns, fns...)
	return els
}

// Scan applies the selector query and scans the result into the given value.
func (els *EmailLogsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, els.ctx, "Select")
	if err := els.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmailLogsQuery, *EmailLogsSelect](ctx, els.EmailLogsQuery, els, els.inters, v)
}

func (els *EmailLogsSelect) sqlScan(ctx context.Context, root *EmailLogsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(els.fns))
	for _, fn := range els.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*els.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := els.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
